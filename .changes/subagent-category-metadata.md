---
default: minor
---

Add first-class `category` metadata support for subagents.

- parse and serialize `category` in agent frontmatter
- preserve `category` through management create/update/detail flows
- expose `category` in agent detail and edit views
- document `category` as delegated-routing intent metadata in subagent docs and schemas
