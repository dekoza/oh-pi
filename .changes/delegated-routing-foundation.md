---
default: minor
---

Add delegated adaptive-routing policy foundations for downstream workflow packages.

- add shared core helpers to read delegated routing policy and resolve categories to available models
- add delegated routing config defaults and schema support to adaptive-routing
- expose delegated routing helpers from `@ifi/oh-pi-core` for reuse by subagents and ant-colony
- document the shared delegated-routing helper surface in the core package README
