---
default: patch
---

Build internal runtime libraries during repo-root installs so git and local checkout package sources can load ant-colony, subagents, plan, and web-remote extensions without missing workspace dist artifacts.
